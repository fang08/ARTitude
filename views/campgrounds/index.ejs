<%- include("../partials/header") %>

<div class="container">
  <header class="jumbotron">
    <div class="container">
      <h1 class="display-4">Welcome To ARTitude!</h1>
      <p class="lead">View our hand-picked artworks from all time.</p>
      <p>
        <a class="btn btn-info btn-lg" href="/campgrounds/new" role="button">Add New Artworks</a>
      </p>
    </div>
  </header>
  
  <div class="row text-center">
    <% for (const camp of campgrounds) { %>
      <div class="col-lg-3 col-md-4 col-sm-6">
        <div class="card border-secondary">
          <img src="<%= camp.image %>" class="card-img-top" alt="<%= camp.name %>">
          <div class="card-body">
            <h5 class="card-title"><%= camp.name %></h5>
            <!-- <p class="card-text"></p> -->
            <a href="/campgrounds/<%= camp._id %>" class="btn btn-info">More Info</a>
          </div>
        </div>
      </div>
    <% } %>
  </div>
</div>

<%- include("../partials/footer") %>